<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sweetsour.dotperfect">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DotPerfect">
        <activity
            android:name=".DotActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.DotActivity"
            android:excludeFromRecents="true"/>
            <!-- dot doesn't show on recents
            but tapping the launcher icon doesn't open the main activity but opens the dot activity
            then another touch opens the main activity-->

        <service
            android:name=".DotService"
            android:description="@string/dot_service_description"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>